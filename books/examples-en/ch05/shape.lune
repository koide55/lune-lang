module shape

# A sum type: a Shape is either a Circle or a Rect.
type Shape =
    | Circle(radius: Double)
    | Rect(width: Double, height: Double)

def area(s: Shape): Double =
    match s:
        | Circle(r) -> r * r * 3.14159
        | Rect(w, h) -> w * h

# A guard adds an extra condition to a pattern.
def describe(s: Shape): String =
    match s:
        | Circle(_) -> "circle"
        | Rect(w, h) if w == h -> "square"
        | Rect(_, _) -> "rectangle"

let circleArea = area(Circle(1.0))

let rectArea = area(Rect(3.0, 4.0))

let squareness = describe(Rect(2.0, 2.0))
